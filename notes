Started by going to github created a repo and copied the url given e.g https://github.com/erinaurelie/wallet-js.git

to commit you do git commit -am "" in the qoutes write your commit message

you have to sepcify where the changes are being made 
git remote add origin https://github.com/erinaurelie/wallet-js.git
the url in the link given by github to your repositor

then after every change after you've cdne your commits you push to github 
git push origin master  

NOTE you need to set up your username and email be fore all this so as to be the author
git config user.name "erinaurelie"

git config user.email "erinaurelieessama@gmail.com"

only the changes that have been commmited will be pushed